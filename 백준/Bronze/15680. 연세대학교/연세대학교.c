#include <stdio.h>

int main()
{
    int N;
    scanf("%d", &N);

    if (N == 0)
    {
        printf("YONSEI");
    }
    else
    {
        printf("Leading the Way to the Future");
    }

    return 0;
}